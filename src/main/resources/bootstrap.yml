spring:
  redis:
    host: localhost
    port: 6379
  kafka:
    bootstrap-servers: localhost:9092
  cloud:
    config:
      enabled: true
  application:
    name: mycrm
  config:
    import: http://localhost:8888
